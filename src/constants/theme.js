import defaultTheme from 'tailwindcss/defaultTheme';

const theme = {
  colors: {
    offWhite: defaultTheme.colors.gray[50],
    darkGray: defaultTheme.colors.gray[800],
    lightGray: defaultTheme.colors.gray[200],
    red: '#E11D48',
    blue: '#2563EB',
  },
};

export default theme;
